
import {
    SELECT_CLIENT,
    SELECT_PRODUCT,
    ACCOUNT_PRODUCTS
} from '../types/index'

const OrderReducer = (state, action) => {
    switch (action.type) {
        default:
            return state
    }
}


export default OrderReducer